### Serverless Golang example

## Invoke local with input
```bash
serverless invoke local --function ip-addresser --path test/input/event.json
```

```bash
serverless invoke local --function geo-locator --path test/input/event.json
```